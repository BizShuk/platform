# TODO

## Buildability

- [ ] 補 `go mod init` 與四個 aws-sdk-go-v2 相依，目前 `go build ./...` 因無 module 直接失敗。

## Behavior

- [ ] 上傳來源 `../tmp` 改為可設定（flag 或環境變數），object key 前綴跟著來源目錄走。
- [ ] 上傳前比對 size 或 ETag 做增量，避免每次重傳整個目錄。
- [ ] 累計逐檔失敗數，以非零 exit code 結束，讓自動化流程能察覺部分失敗。

## Maintenance

- [ ] `aws.EndpointResolverWithOptionsFunc` 與 `config.WithEndpointResolverWithOptions` 已 deprecated，改用 `BaseEndpoint`。
- [ ] 公開 R2 dev URL 目前寫死在結尾輸出，改為由設定推導。

## Archive
